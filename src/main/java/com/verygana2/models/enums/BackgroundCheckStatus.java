package com.verygana2.models.enums;

/** Espeja los valores de "status" que devuelve ZapSign para un check de antecedentes. */
public enum BackgroundCheckStatus {
    NOT_STARTED,
    IN_PROGRESS,
    DELAYED,
    ERROR,
    COMPLETED
}
